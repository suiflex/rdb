name: rdb-driver-oracle

on:
  push:
    branches: [develop, main]
    paths:
      - crates/driver-oracle/**
      - crates/core/**
      - Cargo.toml
      - Cargo.lock
      - rust-toolchain.toml
      - .github/workflows/rdb-driver-oracle.yml
  pull_request:
    paths:
      - crates/driver-oracle/**
      - crates/core/**
      - Cargo.toml
      - Cargo.lock
      - rust-toolchain.toml
      - .github/workflows/rdb-driver-oracle.yml

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: rdb-driver-oracle
      - run: cargo fmt -p rdb-driver-oracle --check
      - run: cargo clippy -p rdb-driver-oracle --all-targets -- -D warnings
      - run: cargo test -p rdb-driver-oracle --lib
