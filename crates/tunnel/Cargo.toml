[package]
name = "rdb-tunnel"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
rdb-core = { path = "../core" }
russh = "0.61.1"
tokio = { version = "1", features = ["full"] }
dirs = "5"
log = "0.4"

[dev-dependencies]
# ponytail: only the known_hosts test needs an RNG, to mint throwaway host keys.
rand = "0.10"
